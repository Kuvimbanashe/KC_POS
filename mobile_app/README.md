# Mobile App

This folder contains all Expo / React Native code for KC POS.

## Key folders
- `app/` screens and routes
- `services/` API, bootstrap, printer helpers
- `store/` Redux slices and types
- `assets/` app assets
- `scripts/` local utility scripts

## Run
```bash
npm install
npx expo start
```
